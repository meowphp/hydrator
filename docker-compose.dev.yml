version: "3"

services:
  php8:
    build:
      args:
        user: vscode
        uid: 1000
      context: ./
      dockerfile: docker/devcontainer/Dockerfile
    working_dir: /app
    volumes:
      - "./:/app"
